<br/>
<%#if current_user.pin_properties.present?%>

<%# @pin_properties.in_groups_of(3) do |group| %>
    <div class="row">
      <% #group.compact.each do |pinboard| %>
          <div class="col-md-4">
                <%#= image_tag pinboard.picture, width: '100%' if pinboard.picture.present?%>
                <h4 style="color: #2196f3;"><%#= pinboard.name %></h4>
            <%#= pinboard.description %>
         # </div>
      <% #end %>
    </div>
<%# end %>
<%# end %>