<%= form_for(:pinboard) do |f| %>
  <% if @pin_properties.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin_properties.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin_properties.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label :name, :class => "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :name, :class => "form-control" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :description, :class => "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_area :description, :class => "form-control" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :picture, :class => "col-sm-2 control-label margin-top" %>
      <div class="col-sm-10">
        <%= f.file_field :picture, :class => "form-control margin-top" %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <br/>
        <p class="pull-right"><%= f.submit :class => "btn btn-primary" %></p>
      </div>
    </div>
<% end %>
